Cleanup: clear tsc strict-index errors - zero typecheck errors, tests green

npx tsc --noEmit reported 16 errors across 5 files (an earlier DEVLOG entry
said "4 in engine.ts" -- that came from a truncated head -5 and was wrong).
All stemmed from noUncheckedIndexedAccess: array and split() indexing yields
T | undefined.

Fixed properly: no @ts-ignore, no tsconfig loosening.
- non-null assertions only where the index is provably in range (loop bounds,
  length > 0 guards), each with a one-line comment saying why
- `as [string, string]` for BASE_QUOTE symbol destructuring -- type-level
  only, so a malformed symbol still falls through to the same "no balance
  found" rejection as before
- one explicit `: number` to break a circular inference in the worker
- one real expression change: key.split(":")[1] -> key.slice(indexOf+1),
  which is provably a string and needs no assertion. Verified equivalent:
  balance keys are `${userId}:${asset}` and assets contain no colon.

engine.ts 11->0, orderProcessor.ts 2->0, projection/worker.ts 1->0,
recover.ts 1->0, test/invariants.ts 1->0. tsc --noEmit now emits nothing.

npm test green, including the dual-engine PARITY section proving the DB and
in-memory engines still produce identical outcomes and balances across all
five matching scenarios. Type-safety only; no logic changed.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
